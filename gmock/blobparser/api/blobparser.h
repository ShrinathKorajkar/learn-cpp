#include <cstdint>
#include <string>
#include <vector>

bool isValid(const std::vector<uint8_t>& blob);
size_t numElements(const std::vector<uint8_t>& blob);
std::string getVendor(const std::vector<uint8_t>& blob);
